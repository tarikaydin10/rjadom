# /etc/systemd/system/rjadom.service
[Unit]
Description=Rjadom
Documentation=https://github.com/tarikaydin10/rjadom
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=rjadom
Group=rjadom
WorkingDirectory=/srv/rjadom
ExecStart=/usr/bin/node /srv/rjadom/server/index.mjs
Restart=on-failure
RestartSec=2

Environment=NODE_ENV=production
Environment=PORT=8787
Environment=HOST=127.0.0.1
Environment=STATIC_DIR=/srv/rjadom/dist
# Outside the deploy directory, so a deploy can never delete your answers.
Environment=DATA_DIR=/var/lib/rjadom
# Holds PAIR_SECRET=... — root-owned, chmod 600. Never in the repository.
EnvironmentFile=/etc/rjadom.env

# The process reads one directory, writes one directory, and answers on one
# loopback port. Nothing else needs to be available to it.
NoNewPrivileges=true
PrivateTmp=true
PrivateDevices=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictAddressFamilies=AF_INET AF_INET6
RestrictNamespaces=true
LockPersonality=true
ReadWritePaths=/var/lib/rjadom
MemoryMax=256M

[Install]
WantedBy=multi-user.target
